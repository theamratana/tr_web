This folder contains a simple static personal website for Theam Ratana.

Files
- index.html — main website
- styles.css — site styles

Quick start
1. Open `index.html` in a browser to view locally.
2. Replace the placeholder email (`your-email@example.com`) and GitHub URL in `index.html`.

Customization
- Edit text directly in `index.html` to update the bio, experience, or links.
- Swap colors by editing `styles.css` variables at the top.

Deployment
- Host as a static site (GitHub Pages, Netlify, Vercel) by pushing these files to a repo.

If you want, I can:
- Replace placeholders with your real email and portfolio link.
- Convert this to a responsive React/Vue site or add more pages.
- Create a GitHub Actions workflow to publish automatically.

Tell me which next step you'd like.